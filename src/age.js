export default class Person {
  constructor(currentAge, pastAge, futureAge) {
  this.currentAge = currentAge;
  this.pastAge = pastAge;
  this.futureAge = futureAge;
}

checkType() {
  const currMerAge = this.currentAge / .24;
  return currMerAge;
  // currVenAge = this.currentAge / .62;
  // currMarAge = this.currentAge * 1.88;
  // currJupAge = this.currentAge * 11.86;
  // futureMerAge = this.futureAge /.24;
  // futureVenAge = this.futureAge / .62;
  // futureMarAge = this.futureAge * 1.88;
  // futureJupAge = this.futureAge * 11.86;
  // pastMerAge = this.pastAge / .24;
  // pastVenAge = this.pastAge / .62;
  // pastMarAge = this.pastAge * 1.88;
  // pastJupAge = this.pastAge * 11.86;

    

  }
}


